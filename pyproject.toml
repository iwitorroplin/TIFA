[project]
name = "tifa"
version = "0.1.0"
description = "TIFA"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.11,<7",
    "PyYAML>=6.0,<7",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
