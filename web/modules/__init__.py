"""Un paquete por módulo de negocio de la parte web, igual que
`src/modules/` en la app de escritorio: cada uno agrupa sus rutas, su tabla
`*_web` (si la tiene) y su propia plantilla. `web/registry.py` es el único
sitio que los conoce a todos a la vez.
"""
